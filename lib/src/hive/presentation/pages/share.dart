import 'package:flutter/material.dart';

class ShareHivePage extends StatelessWidget {
  const ShareHivePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Share Hive Page')),
    );
  }
}
